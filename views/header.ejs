<header id="scrollHeader">
    <a href="/" class="logo"><img src="../img/logo.png" alt=""></a>
    <ul class="navbar">
        <li><a class="active1" href="/">Trang chủ</a></li>
        <li><a href="/shop">Cửa hàng</a></li>
        <li><a href="/blog">Giới thiệu</a></li>
        <li><a href="/contact">Liên hệ</a></li>
        <li><a href="/page">Bài viết</a></li>
    </ul>
    <div class="main">
        <a href="/cart" class="cart-menu">
            <ion-icon name="cart-outline"></ion-icon>
        </a>

        <a href="/login" class="user-menu">
            <ion-icon name="person-outline"></ion-icon>
        </a>

        <div class="bx bx-menu" id="menu-icon">
            <ion-icon name="menu-outline"></ion-icon>
        </div>
    </div>
</header>

<script>
    const account = JSON.parse(localStorage.getItem('account'));
    const main = document.querySelector('.main');
    console.log(account[0].username);
    if (!account) {
        console.log('jk');
    } else {
        main.innerHTML = `
            <a href="/cart" class="cart-menu">
                <ion-icon name="cart-outline"></ion-icon>
            </a>

            <a href="" class="user-menu" ">
                <span>${account[0].username}
                    <ul id = 'dropdown'>
                    </ul>
                </span>
            </a>

            <div class="bx bx-menu" id="menu-icon">
                <ion-icon name="menu-outline"></ion-icon>
            </div>
        `;
    }

    const dropdown = document.querySelector('#dropdown');
    if (account[0].role == 'admin') {
        dropdown.innerHTML = `
            <li><a href="/meInfor">Thông Tin Cá Nhân</a></li>
            <li><a href="/admin">Admin</a></li>
            <li><p class="btnOut">Đăng xuất</p></li>
        `
    } else {
        dropdown.innerHTML = `
            <li><a href="/meInfor">Thông Tin Cá Nhân</a></li>
            <li><p class="btnOut">Đăng xuất</p></li>
        `
    }

    const logOut = document.querySelector('.btnOut');
    logOut.addEventListener('click', () => {
        localStorage.removeItem('account')
        window.location.href = '/login';
        console.log('hi');
    })
</script>